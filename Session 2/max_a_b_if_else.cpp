#include<iostream>

using namespace std;
int main()
	{
		int a,b,max;
		cout <<"Nhap a: ";
		cin >> a;
		cout << "Nhap b: ";
		cin >> b;
		max = b;
		if(a>b)
			cout << a <<" la so lon nhat.";
			
		else
			cout <<b << " la so lon nhat";
		return 0;
	}
